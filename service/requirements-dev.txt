# Enough to run the unit + API tests without installing PaddleOCR.
# The integration test additionally needs: paddleocr, paddlepaddle, pillow.
fastapi>=0.110
python-multipart>=0.0.9
httpx>=0.27
pytest>=8.0
